import {setPhoneNumber} from './phoneNumber';
import {setConfirm} from './confirm';
import {setRole} from './role';
import {destroySession} from './destroySession';

const allActions = {
  setPhoneNumber,
  setConfirm,
  setRole,
  destroySession
};

export default allActions;